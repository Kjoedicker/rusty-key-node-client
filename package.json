{
  "name": "rusty-key-client",
  "version": "1.0.0",
  "description": "Node client for interfacing with the rusty-key command server",
  "main": "index.js",
  "scripts": {
    "test": "mocha --exit -r ts-node/register test/**/*.ts",
    "test-watch": "nodemon --exec mocha --exit -r ts-node/register test/*.ts",
    "run-client": "nodemon --exec ts-node client.ts"
  },
  "author": "Keifer Joedicker",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.3.6",
    "chai-as-promised": "^7.1.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.4"
  },
  "devDependencies": {
    "@types/node": "^18.16.1",
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "nodemon": "^2.0.22"
  }
}
